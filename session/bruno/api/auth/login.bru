meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/login
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "admin"
  }
}

script:post-response {
  console.log("res", res);
  bru.setEnvVar("token", res.body.data.token);
}
